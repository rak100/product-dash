{
  "name": "product_api",
  "version": "1.0.0",
  "description": "Api for product",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run:server\" \"npm run start:client\"",
    "start:server":"cd backend && node server.js",
    "start:client":"cd frontend && npm start",
    "build:client":"cd frontend npm run build",
    "start:prod":"cd backend && npm run build:client && npm run start"

  },
  "keywords": [
    "movie"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "imdb-api": "^4.4.1",
    "jsonwebtoken": "^8.5.1",
    "jwt-decode": "^3.1.2",
    "mongoose": "^5.11.7",
    "multer": "^1.4.2"
  }
}
